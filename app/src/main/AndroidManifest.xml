<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.javacakegames.pride">

  <uses-feature android:name="android.hardware.audio.output" />

  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

  <application
    android:icon="@mipmap/ic_launcher"
    android:label="Pride Piano"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

    <!-- todo https://stackoverflow.com/a/28644266/ -->
    <!-- android:screenOrientation="landscape" -->
    <activity
      android:name=".activities.PrideActivity"
      android:exported="true"

      android:excludeFromRecents="true"
      android:launchMode="singleTask"
      android:configChanges="density|fontScale|keyboard|keyboardHidden|layoutDirection|locale|mcc|mnc|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|colorMode|fontWeightAdjustment">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
    </activity>

    <activity
        android:name=".activities.PlainActivity"
        android:label="Plain Piano"
        android:exported="true"
        android:screenOrientation="landscape"
        android:launchMode="singleTask"
        android:configChanges="density|fontScale|keyboard|keyboardHidden|layoutDirection|locale|mcc|mnc|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|colorMode|fontWeightAdjustment">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

  </application>

</manifest>